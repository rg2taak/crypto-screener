import Vue from "vue";
import * as Vuex from "vuex";

Vue.use(Vuex);

/**
 * state
 */
const state = () => ({});

/**
 * getters
 */
const getters = {};

/**
 * Mutations
 */
const mutations = {};

/**
 * Actions
 */
const actions = {};

export default new Vuex.Store({
    state,
    getters,
    mutations,
    actions,
});
